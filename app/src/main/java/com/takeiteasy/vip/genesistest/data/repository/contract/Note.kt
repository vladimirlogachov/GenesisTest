package com.takeiteasy.vip.genesistest.data.repository.contract

interface Note {
    companion object {
        val readme = "Actually I can't understand why do you need interfaces in the package. They seem to be redundant"
    }
}
